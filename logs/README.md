# Logs Directory

This directory contains log files for the Agent Memory MCP System.

## Log Files

- `mcp_server.log` - MCP server logs
- `daemon.log` - Global daemon logs (when run locally)

## Note

Log files are excluded from version control via `.gitignore`.

For production deployments, logs are stored in `~/.agent-memory-mcp/logs/`.
